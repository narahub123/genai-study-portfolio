interface IHeader {
  title: string;
  header: string;
  expl: string;
}

import type {
  FontSizeToken,
  SizeToken,
  RoundedToken,
  ColorToken,
} from "./token";

export type { IHeader, FontSizeToken, SizeToken, RoundedToken, ColorToken };
